@page "/meters"
@attribute [Navigation(Icon= Icons.Material.Outlined.ElectricMeter, Title = "Measurement locations", Allows = new[] { RoleModel.LocationRepresentative, RoleModel.NetworkUserRepresentative, RoleModel.OperatorRepresentative })]

@if (user is null)
{
  return;
}

<MetersTable/>

@code {

  [CascadingParameter]
  public UserState user { get; set; } = default!;

}
