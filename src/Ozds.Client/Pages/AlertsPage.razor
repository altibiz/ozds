@page "/alerts"
@attribute [Navigation(Icon= Icons.Material.Outlined.Notifications, Title = "Alarms", Allows = new[] { RoleModel.OperatorRepresentative })]


@if (user is null)
{
  return;
}

<AlertsTable/>

@code {

  [CascadingParameter]
  public UserState user { get; set; } = default!;

  [CascadingParameter]
  public RepresentativeState RepresentativeState { get; set; } = default!;

}
