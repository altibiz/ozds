@page "/messengers"
@attribute [Navigation(Icon= Icons.Material.Outlined.Message, ParentTitle = "Alerts", Title = "Messengers", Allows = new[] { RoleModel.OperatorRepresentative })]

@if (user is null)
{
  return;
}

<MessengersTable/>

@code {

  [CascadingParameter]
  public UserState user { get; set; } = default!;

}
