@page "/network-users"
@attribute [Navigation(Icon= Icons.Material.Outlined.AccountTree, ParentTitle = "Tables", Title = "Network users", Allows = new[] { RoleModel.LocationRepresentative, RoleModel.NetworkUserRepresentative, RoleModel.OperatorRepresentative })]

@if (user is null)
{
  return;
}

<NetUsersTable/>

@code {

  [CascadingParameter]
  public UserState user { get; set; } = default!;

}
