@page "/network-users"
@attribute [Navigation(Title = "Korisnici mereže", Allows = new[] { RoleModel.LocationRepresentative, RoleModel.NetworkUserRepresentative, RoleModel.OperatorRepresentative })]

@if (user is null)
{
  return;
}

<NetUsersTable/>

@code {
  [CascadingParameter] public UserState user { get; set; } = default!;
}
