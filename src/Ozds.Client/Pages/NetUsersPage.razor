@page "/network-users"
@attribute [Navigation(Icon= Icons.Material.Outlined.AccountTree, Title = "Korisnici mreže", Allows = new[] { RoleModel.LocationRepresentative, RoleModel.NetworkUserRepresentative, RoleModel.OperatorRepresentative })]

@if (user is null)
{
  return;
}

<NetUsersTable/>

@code {
  [CascadingParameter] public UserState user { get; set; } = default!;
}
