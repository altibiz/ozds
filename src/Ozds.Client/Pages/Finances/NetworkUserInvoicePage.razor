@namespace Ozds.Client.Pages

@page "/finances/network-user-invoice/{id?}"
@using MudBlazor
@using Ozds.Business.Models.Abstractions
@using Ozds.Business.Models.Composite
@using Ozds.Client.Components.Models
@using Ozds.Client.Components.Streaming
@inherits Ozds.Client.Components.Models.Base.OzdsIdentifiableModelPageComponentBase<Ozds.Business.Models.Abstractions.INetworkUserInvoice>

<MappedMutating
  T="StatefulNetworkUserInvoiceModel"
  TMapped="INetworkUserInvoice"
  LoadAsync="OnLoadAsync"
  Map="@(model => model.Invoice)"
  AsReadonly
  NotFoundOnCreate
  WithTitle
  WithHeading>
  <Details>
    @if (context.Model.State is not null)
    {
      <Details Model="@context.Model.State"/>
    }

    <div class="mt-4">
      <Details Model="@context.Model.Invoice"/>
    </div>

    <div class="mt-4">
      <MudHidden Breakpoint="Breakpoint.SmAndDown">
        <MudButton
          Variant="Variant.Outlined"
          Color="Color.Primary"
          Href="@(PageHref<NetworkUserInvoiceDocumentPage>(new { context.Model.Invoice.Id }))">
          @Translate("Document")
        </MudButton>
      </MudHidden>

      <MudButton
        Class="ml-4"
        Variant="Variant.Outlined"
        Color="Color.Primary"
        Href="@($"/app/document/network-user-invoice/{context.Model.Invoice.Id}")">
        @Translate("Document (PDF)")
      </MudButton>
    </div>
  </Details>
</MappedMutating>

@code {

  [Parameter]
  public string Id { get; set; } = default!;

}
