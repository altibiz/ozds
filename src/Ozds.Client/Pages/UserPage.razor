@page "/user/{id}"
@attribute [Navigation(RouteValue = "id", Parents = new[] { typeof(RepresentativesPage) })]
@attribute [Authorize]
@attribute [StreamRendering]
@inherits OwningComponentBase<Ozds.Business.Queries.OzdsRepresentativeQueries>

<Loading
  Load="@(async () => (await Service.MaybeRepresentingUserByUserId(Id))!)">
  <Found>
    @context
  </Found>
</Loading>

@code {
  [Parameter] public string Id { get; set; } = default!;
}
