@page "/network-user/{id}"
@attribute [Navigation(RouteValue = "id", Parents = new[] { typeof(UsersPage) })]
@attribute [StreamRendering]

@* <NetUserView Id="@Id"/> *@
<BlueLowCatalogueMutate Id="@Id"/>

@code {
  [Parameter] public string? Id { get; set; }
}
