@page "/network-user/{id}"
@attribute [Navigation( Title = "Form", Parents = new[] { typeof(UsersPage) })]
@attribute [StreamRendering]

@* <NetUserView Id="@Id"/> *@
<WhiteMediumCatalogueMutate Id="@Id"/>

@code {
  [Parameter] public string? Id { get; set; } = "-1";
}
