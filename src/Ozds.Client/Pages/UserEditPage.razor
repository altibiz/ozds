@page "/user/{id}/edit"
@attribute [Navigation(
  Title = "User",
  RouteValue = "id",
  Parents = new[] { typeof(UsersPage) })]
@attribute [Authorize]
@attribute [StreamRendering]

<MaybeRepresentingUserEdit Id="@Id"/>

@code {

  [Parameter]
  public string? Id { get; set; }

}
