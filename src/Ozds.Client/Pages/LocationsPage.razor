@page "/locations"
@attribute [Navigation(Title = "Lokacije", Allows = new[] { RoleModel.LocationRepresentative, RoleModel.OperatorRepresentative })]

@if (user is null)
{
  return;
}

<LocationsTable/>

@code {
  [CascadingParameter] public UserState user { get; set; } = default!;
}
