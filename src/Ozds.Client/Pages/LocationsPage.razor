@page "/locations"
@attribute [Navigation(
Icon= Icons.Material.Outlined.LocationOn, Title = "Locations", Allows = new[] { RoleModel.LocationRepresentative, RoleModel.OperatorRepresentative })]

@if (user is null)
{
  return;
}

<LocationsTable/>

@code {
  [CascadingParameter] public UserState user { get; set; } = default!;
}
