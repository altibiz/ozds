@page "/location/{Id}"


@* @if (user is null)
{
  return;
}
@if (_location != null)
{
  <MudContainer MaxWidth="MaxWidth.ExtraSmall" Class="d-flex  flex-column" Style="float: left;">
    <MudText Class="d-flex align-center my-2" Typo="@Typo.h5">@_location.Title</MudText>
    <MudPaper class="mt-2 px-4 py-2">
      <MudGrid Spacing="3">

        <MudItem xs="12" sm="6">
          <MudText Typo="Typo.body1" Style="font-weight: bold;">
            @T["Title"]:
          </MudText>
        </MudItem>
        <MudItem xs="12" sm="6">
          <MudHidden Breakpoint="Breakpoint.Xs">
            <MudText Type="Typo.body2" Align="MudAlign.Right">
              @_location.Title
            </MudText>
          </MudHidden>
          <MudHidden Breakpoint="Breakpoint.SmAndUp">
            <MudText Type="Typo.body2" Align="MudAlign.Left">
              @_location.Title
            </MudText>
          </MudHidden>
        </MudItem>

      </MudGrid>
    </MudPaper>
  </MudContainer>
} *@


<LocationMutate Id="@Id"/>

@code {
  [Parameter] public string? Id { get; set; }
  [CascadingParameter] public UserState user { get; set; } = default!;
  private LocationModel? _location;

  protected override async Task OnInitializedAsync()
  {
    // !!! Find _location by id
  }

}
