@namespace Ozds.Client.Components.Models

@typeparam TPrefix
@typeparam TModel where TModel : Ozds.Business.Models.Base.MeterModel
@using Ozds.Business.Models.Abstractions
@using Ozds.Business.Models.Base
@inherits Ozds.Client.Components.Models.Base.OzdsEditComponentBase<TPrefix, TModel>

@* TODO: prevent user error based on type and naming convention here *@
@Field(x => x.Id)

@(Base<IdentifiableModel>())

@(LinkField<IMessenger>(x => x.MessengerId))

@Field(x => x.ConnectionPower_W)

@Field(x => x.Phases)
