@using MudBlazor
@using Ozds.Business.Models.Complex
@using Ozds.Client.Components.Models.Base

@namespace Ozds.Client.Components.Models.Implementations.Administration

@typeparam TPrefix
@typeparam TModel where TModel : PhysicalPersonModel
@inherits OzdsColumnsComponentBase<TPrefix, TModel>

<PropertyColumn
  T="TPrefix"
  TProperty="string"
  Property="@(x => Fix(x)!.Name)"
  Title='@Translate("Person name")'>
  <CellTemplate>
    <MudText>
      @(Fix(context.Item)?.Name)
    </MudText>
  </CellTemplate>
</PropertyColumn>
