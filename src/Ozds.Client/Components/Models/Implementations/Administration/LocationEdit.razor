@using Microsoft.AspNetCore.Components
@using MudBlazor
@using Ozds.Business.Models
@using Ozds.Business.Models.Abstractions
@using Ozds.Client.Components.Models.Base
@using Ozds.Client.Components.Fields
@using Ozds.Client.Components.Streaming

@namespace Ozds.Client.Components.Models.Implementations.Administration

@typeparam TPrefix
@typeparam TModel where TModel : LocationModel
@inherits OzdsEditComponentBase<TPrefix, TModel>

@(Base<IIdentifiable>())

<Edit Model="@Get(x => x.LegalPerson)" />

@Field(x => x.AltiBizSubProjectCode)

<MudSelect
  Label="@Translate("White Medium Network User Catalogue")"
  Value="@Get(x => x.WhiteMediumNetworkUserCatalogueId)"
  ValueChanged="@Set(x => x.WhiteMediumNetworkUserCatalogueId)"
  For="@For(x => x.WhiteMediumNetworkUserCatalogueId)">
  <Paging T="WhiteMediumNetworkUserCatalogueModel">
    <Summary Context="model">
      <MudSelectItem Value="@model.Id">
        <Summary Model="@model"/>
      </MudSelectItem>
    </Summary>
  </Paging>
</MudSelect>

<MudSelect
  Label="@Translate("Blue Low Network User Catalogues")"
  Value="@Get(x => x.BlueLowNetworkUserCatalogueId)"
  ValueChanged="@Set(x => x.BlueLowNetworkUserCatalogueId)"
  For="@For(x => x.BlueLowNetworkUserCatalogueId)">
  <Paging T="BlueLowNetworkUserCatalogueModel">
    <Summary Context="model">
      <MudSelectItem Value="@model.Id">
        <Summary Model="@model"/>
      </MudSelectItem>
    </Summary>
  </Paging>
</MudSelect>

<MudSelect
  Label="@Translate("White Low Network User Catalogues")"
  Value="@Get(x => x.WhiteLowNetworkUserCatalogueId)"
  ValueChanged="@Set(x => x.WhiteLowNetworkUserCatalogueId)"
  For="@For(x => x.WhiteLowNetworkUserCatalogueId)">
  <Paging T="WhiteLowNetworkUserCatalogueModel">
    <Summary Context="model">
      <MudSelectItem Value="@model.Id">
        <Summary Model="@model"/>
      </MudSelectItem>
    </Summary>
  </Paging>
</MudSelect>

<MudSelect
  Label="@Translate("Red Low Network User Catalogues")"
  Value="@Get(x => x.RedLowNetworkUserCatalogueId)"
  ValueChanged="@Set(x => x.RedLowNetworkUserCatalogueId)"
  For="@For(x => x.RedLowNetworkUserCatalogueId)">
  <Paging T="RedLowNetworkUserCatalogueModel">
    <Summary Context="model">
      <MudSelectItem Value="@model.Id">
        <Summary Model="@model"/>
      </MudSelectItem>
    </Summary>
  </Paging>
</MudSelect>

<MudSelect
  Label="@Translate("Regulatory Network User Catalogues")"
  Value="@Get(x => x.RegulatoryCatalogueId)"
  ValueChanged="@Set(x => x.RegulatoryCatalogueId)"
  For="@For(x => x.RegulatoryCatalogueId)">
  <Paging T="RegulatoryCatalogueModel">
    <Summary Context="model">
      <MudSelectItem Value="@model.Id">
        <Summary Model="@model"/>
      </MudSelectItem>
    </Summary>
  </Paging>
</MudSelect>
