@using MudBlazor
@using Ozds.Business.Models
@using Ozds.Business.Models.Complex
@using Ozds.Client.Components.Models.Base

@namespace Ozds.Client.Components.Models

@typeparam TPrefix
@typeparam TModel where TModel : WhiteLowNetworkUserCalculationModel
@inherits OzdsColumnsComponentBase<TPrefix, TModel>

<PropertyColumn
  T="TPrefix"
  TProperty="string"
  Property="x => Raw(x)!.Title"
  Title='@Translate("Title")'>
  <CellTemplate>
    <MudText>@Raw(context.Item)?.Title</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="string"
  Property="x => Raw(x)!.MeterId"
  Title='@Translate("Meter ID")'>
  <CellTemplate>
    <MudText>@Raw(context.Item)?.MeterId</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="DateTimeOffset"
  Property="x => Raw(x)!.FromDate"
  Title='@Translate("From Date")'>
  <CellTemplate>
    <MudText>@DateTimeString(Raw(context.Item)?.FromDate)</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="DateTimeOffset"
  Property="x => Raw(x)!.ToDate"
  Title='@Translate("To Date")'>
  <CellTemplate>
    <MudText>@DateTimeString(Raw(context.Item)?.ToDate)</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Raw(x)!.UsageActiveEnergyTotalImportT1.Total_EUR"
  Title='@Translate("Usage Active Energy Import T1 Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Raw(context.Item)?.UsageActiveEnergyTotalImportT1.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Raw(x)!.UsageActiveEnergyTotalImportT2.Total_EUR"
  Title='@Translate("Usage Active Energy Import T2 Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Raw(context.Item)?.UsageActiveEnergyTotalImportT2.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Raw(x)!.UsageReactiveEnergyTotalRampedT0.Total_EUR"
  Title='@Translate("Usage Reactive Energy Total Ramped T0 Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Raw(context.Item)?.UsageReactiveEnergyTotalRampedT0.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Raw(x)!.Total_EUR"
  Title='@Translate("Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Raw(context.Item)?.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>
