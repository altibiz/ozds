@using MudBlazor
@using Ozds.Business.Models
@using Ozds.Business.Models.Complex
@using Ozds.Client.Components.Models.Base

@typeparam TPrefix
@typeparam TModel where TModel : BlueLowNetworkUserCalculationModel
@inherits OzdsColumnsComponentBase<TPrefix, TModel>

<PropertyColumn
  T="TPrefix"
  TProperty="string"
  Property="x => Fix(x)!.Title"
  Title='@Translate("Title")'>
  <CellTemplate>
    <MudText>@Fix(context.Item)?.Title</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="string"
  Property="x => Fix(x)!.MeterId"
  Title='@Translate("Meter ID")'>
  <CellTemplate>
    <MudText>@Fix(context.Item)?.MeterId</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="DateTimeOffset"
  Property="x => Fix(x)!.FromDate"
  Title='@Translate("From Date")'>
  <CellTemplate>
    <MudText>@DateTimeString(Fix(context.Item)?.FromDate)</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="DateTimeOffset"
  Property="x => Fix(x)!.ToDate"
  Title='@Translate("To Date")'>
  <CellTemplate>
    <MudText>@DateTimeString(Fix(context.Item)?.ToDate)</MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Fix(x)!.UsageActiveEnergyTotalImportT0.Total_EUR"
  Title='@Translate("Usage Active Energy Import T0 Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Fix(context.Item)?.UsageActiveEnergyTotalImportT0.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Fix(x)!.UsageReactiveEnergyTotalRampedT0.Total_EUR"
  Title='@Translate("Usage Reactive Energy Total Ramped T0 Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Fix(context.Item)?.UsageReactiveEnergyTotalRampedT0.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Fix(x)!.SupplyActiveEnergyTotalImportT1.Total_EUR"
  Title='@Translate("Supply Active Energy Import T1 Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Fix(context.Item)?.SupplyActiveEnergyTotalImportT1.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Fix(x)!.SupplyActiveEnergyTotalImportT2.Total_EUR"
  Title='@Translate("Supply Active Energy Import T2 Total (€)")'>
  <CellTemplate>
    <MudText>
      @NumericString(Fix(context.Item)?.SupplyActiveEnergyTotalImportT2.Total_EUR)
    </MudText>
  </CellTemplate>
</PropertyColumn>

<PropertyColumn
  T="TPrefix"
  TProperty="decimal"
  Property="x => Fix(x)!.Total_EUR"
  Title='@Translate("Total (€)")'>
  <CellTemplate>
    <MudText>@NumericString(Fix(context.Item)?.Total_EUR)</MudText>
  </CellTemplate>
</PropertyColumn>
