@using Ozds.Business.Models.Abstractions
@using Ozds.Client.Components.Models.Base

@namespace Ozds.Client.Components.Models

@inherits OzdsSummaryComponentBase<IFinancial>

@(Base<IIdentifiable>())

@Subtitle(x => DateString(x.FromDate))

@Subtitle(x => DateString(x.ToDate))
