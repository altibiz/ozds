@using Ozds.Client
@using Ozds.Server.ViewModels

@namespace Ozds.Server.Views.App

@model AppViewModel

@{
  Layout = "_Layout.cshtml";
}

@(await Html.RenderComponentAsync<App>(
  RenderMode.Server,
  new {
    Culture = Model.Culture?.TwoLetterISOLanguageName,
    Model.LogoutToken
  }
))
